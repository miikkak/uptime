---
name: Gradle Lockfile Refresh

on: # yamllint disable-line rule:truthy
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  refresh-lockfile:
    name: Gradle Lockfile Refresh
    uses: miikkak/workflows/.github/workflows/gradle-lockfile-refresh.yml@878ac92905995ab976d4814af0e919306f0d3ccc # main
    secrets:
      PULL_REQUEST_TOKEN: ${{ secrets.PULL_REQUEST_TOKEN }}
